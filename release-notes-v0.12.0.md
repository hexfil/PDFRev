# PDFRev v0.12.0

PDFRev 的 **Tauri 2 重写版**，界面支持**简体中文 / 英文**（英文标题 PDF Revisor）。
界面逻辑与 Electron 版同一份代码，后端换成 Rust（lopdf）——单个 exe 即可运行。

## 下载

- `PDFRev.exe` — 单文件免安装，4,703,232 字节（4.49 MB）。双击即用，拷到 U 盘也行。
- `LICENSE` — MIT 许可原文。

> 依赖系统 WebView2（Windows 10 1803+ 通常已预装）。若提示缺失，安装
> [Microsoft Edge WebView2 Runtime](https://developer.microsoft.com/microsoft-edge/webview2/) 即可。

## 本版新增（相对 v0.1.0）

| 新增 | 说明 |
|---|---|
| **多语言界面** | 顶栏语言选择框，简中 / 英文即时切换。工具栏、页面面板、右侧四张卡片、拖拽提示、预览、确认框、命令行帮助、版权页条款、缩略图角标、顶栏文件信息**全部跟着变** |
| **英文标题 PDF Revisor** | 网页标题、窗口标题栏、任务栏三处同步切换（切换语言时立刻生效） |
| **后端错误本地化** | Rust 侧不再返回中文，改为返回语言无关的 `{code, ekey, eargs}`，由前端按当前语言渲染；加语言不用改 Rust、不用重编译 |
| 语言记忆 | 选择记在 localStorage，下次启动保持；首次启动跟随系统语言 |
| 测试基线 | 界面自检 69 → **89 项**（新增 20 项国际化 / 窗口标题断言） |

## 功能（与原版一致）

| 功能 | 说明 |
|---|---|
| 打开 PDF | 按钮选择，或直接把文件**拖进窗口**（拖入走内存，不落地临时文件） |
| 缩略图 | PDF.js 渲染，可勾选 |
| 删除页面 | 单页 / 多页 / 范围表达式（`3-5,8,12-end`，也认中文 `至`、`，`） |
| 重排页面 | 拖动缩略图，或直接输入页序（`2,1,3`） |
| 提取为新 PDF | 把选中页导出成新文件 |
| 旋转 | 90° / 180° / 270° |
| 插入 PDF | 在首页前 / 尾页后 / 第 N 页前或后插入另一个文件 |
| 反转页序、撤销 | |
| 页面预览 | 双击缩略图打开；预览内按 **Delete** 删当前页；**滚轮缩放**（以光标为中心） |
| 顶栏信息 | 文件名 · 页数 · 体积 · **完整路径** · 创建时间 · 修改时间 |
| 顶栏品牌 | logo + 加黑加粗的 PDFRev，位于「版权」按钮左侧 |
| 只读文件 | 保存前询问，确认后解除只读再写 |
| 原子保存 | 临时文件 + 改名，写入中断不会毁掉原文件 |
| 另存为 | |
| 等价命令行 | 界面把当前操作翻译成命令行并展示，一键复制；内置「帮助」面板列出 7 个命令的用法与 9 个示例（随语言切换） |
| 版权与许可 | 首次启动自动弹出，采用 MIT 协议 |

## 效率对比

| | Electron 版 | 本版（Tauri 2） |
|---|---|---|
| 发布体积 | 7z 61.44 MB / 解压 233 MB | **单 exe 4.49 MB** |
| 相对体积 | 100% | **1.93%** |
| 运行时 | 自带 Chromium + Node | 系统 WebView2 |

## 验证情况

- Rust 单元测试 **14 项通过**（页码表达式、插入位置、删/排/取/转/插、元数据保留含 UTF-16 中文标题）
- 界面端到端自检 **89 项通过**（真实 WebView2）：版权页 → 打开 → 缩略图 → 顶栏信息 → 双击预览 → 滚轮缩放 → 预览内 Delete 删页 → 排序 → 撤销 → 旋转 → 保存 → stat → 剪贴板 → 62 页真实文档全量渲染 → 国际化（切语言 / 文案 / 后端错误 / 原生窗口标题）→ 无未捕获错误
- 真实文档 `VPEg.pdf`（62 页 / 10.35 MB）端到端通过，**源文件 sha256 未被改写**
- 空目录便携复验：把 exe 单独放进空文件夹，89 项自检全绿

## 校验

```
PDFRev.exe
大小  4,703,232 字节（4.49 MB）
sha256  994CA77A12C2868FD11106E025AD3EA5DCA82BDA7279D6A12F4BBFAD947AB49F
```

## 许可

MIT。见 [LICENSE](https://github.com/woxii88/PDFRev_Tauri/blob/master/LICENSE)。